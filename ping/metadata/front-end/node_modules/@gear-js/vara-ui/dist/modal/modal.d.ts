import { ReactNode } from 'react';
type Props = {
    heading: string;
    close: () => void;
    children?: ReactNode;
    className?: string;
    footer?: ReactNode;
};
declare const Modal: ({ heading, close, children, className, footer }: Props) => import("react").ReactPortal | null;
export { Modal };
export type { Props as ModalProps };
