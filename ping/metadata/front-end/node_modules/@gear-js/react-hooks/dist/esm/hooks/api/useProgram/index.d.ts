import { ProgramMetadata } from '@gear-js/api';
import { Code, CodeId, UseProgram } from './types';
declare function useUploadProgram(code: Code | undefined, metadata?: ProgramMetadata | undefined, payloadType?: string | undefined): UseProgram;
declare function useCreateProgram(codeId: CodeId | undefined, metadata?: ProgramMetadata | undefined, payloadType?: string | undefined): UseProgram;
export { useUploadProgram, useCreateProgram };
