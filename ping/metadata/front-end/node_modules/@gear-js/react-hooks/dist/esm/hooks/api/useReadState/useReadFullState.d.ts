import { ProgramMetadata } from '@gear-js/api';
import { AnyJson } from '@polkadot/types/types';
import { HexString } from '@polkadot/util/types';
declare function useReadFullState<T = AnyJson>(programId: HexString | undefined, meta: ProgramMetadata | undefined, payload: AnyJson, isReadOnError?: boolean): {
    state: T | undefined;
    isStateRead: boolean;
    error: string;
};
export { useReadFullState };
