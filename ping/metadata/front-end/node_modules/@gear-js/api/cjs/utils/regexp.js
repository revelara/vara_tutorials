'use strict';

const REGULAR_EXP = {
    endWord: /\b[\w:]+\b/g,
    angleBracket: /<.+>/,
    roundBracket: /^\(.+\)$/,
    squareBracket: /^\[.+\]$/,
    generic: /\b\w+</g,
};

exports.REGULAR_EXP = REGULAR_EXP;
