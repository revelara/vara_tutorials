'use strict';

var utilCrypto = require('@polkadot/util-crypto');

function signatureIsValid(publicKey, signature, message) {
    return utilCrypto.signatureVerify(message, signature, publicKey).isValid;
}

exports.signatureIsValid = signatureIsValid;
