'use strict';

var types = require('@polkadot/types');

class GearEventData extends types.GenericEventData {
    constructor(data) {
        super(data.registry, data.toU8a(), data.meta, data.section, data.method);
    }
}

exports.GearEventData = GearEventData;
