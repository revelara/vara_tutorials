/// <reference types="node" />
import { BlockNumber } from '@polkadot/types/interfaces';
import { u64 } from '@polkadot/types';
import { IGearPages } from '../types';
export declare function readState(wasmBytes: Buffer, initialSize: number, pages: IGearPages, inputValue?: Uint8Array, blockTimestamp?: u64, blockNumber?: BlockNumber): Promise<Uint8Array>;
